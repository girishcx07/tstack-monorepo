{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "env": ["PUBLIC_*", "SERVER_AUTH_SECRET", "DB_POSTGRES_URL"],
      "inputs": ["$TURBO_DEFAULT$", ".env"],
      "persistent": true
    },
    "build": {
      "env": ["PUBLIC_*", "SERVER_AUTH_SECRET", "DB_POSTGRES_URL"],
      "inputs": ["$TURBO_DEFAULT$", ".env"]
    },
    "start": {
      "env": ["PUBLIC_*", "SERVER_AUTH_SECRET", "DB_POSTGRES_URL"],
      "persistent": true
    }
  }
}
