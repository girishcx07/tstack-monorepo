# Used for www application — TanStack Start SSR app
# This should be passed as a build-time variable (ARG) in docker.
VITE_PUBLIC_SERVER_URL=http://localhost:3035
VITE_PUBLIC_SERVER_API_PATH=/api

# This app's own public URL (note: port 3001 avoids conflicts with other services)
VITE_PUBLIC_WEB_URL=http://localhost:3001

# Better Auth — this app runs its own auth instance connected to the same DB
SERVER_AUTH_SECRET=changeme_to_at_least_32_chars_in_production!!

# Database — shared with the server app
DB_POSTGRES_URL=postgres://postgres:postgres@localhost:5432/postgres
